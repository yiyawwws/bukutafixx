require('dotenv').config(); const { DisputeController } = require('./src/controllers/MiscControllers'); const pool = require('./src/config/database'); async function test() { const req = { user: { id: 1, role: 'buyer' }, body: { order_id: 1, reason: 'Test reason' }, file: { buffer: Buffer.from('test'), mimetype: 'video/mp4' } }; const res = { status: (code) => ({ json: (data) => console.log('Response:', code, data) }) }; await DisputeController.reportDispute(req, res); process.exit(0); } test();
