// Route ini DEPRECATED — chat internal ada di /api/chats (chats.js)
// Untuk kontak penjual, gunakan GET /api/books/:id/seller-contact
const express = require('express');
const router = express.Router();
module.exports = router;
