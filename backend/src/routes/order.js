// Route ini DEPRECATED — sudah digabung ke /api/orders (orders.js)
// File ini ditinggal untuk backward compatibility tapi tidak diregister di index.js
const express = require('express');
const router = express.Router();
module.exports = router;
